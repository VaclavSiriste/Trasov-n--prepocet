[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Healthcheck NENÍ v toml – sync cron služba nemá HTTP server.
# U webové služby nastav v Railway UI: Healthcheck Path = /api/health
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
